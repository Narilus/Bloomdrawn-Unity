# Frozen Agent Task Packets

The Bloom Planner may write one bounded task packet here. Once approved for implementation, commit or otherwise freeze the packet before invoking the Bloom Builder.

The Builder is denied edits to this directory. The Auditor treats the packet as the task's acceptance authority below the repository's established design/decision/implementation-plan hierarchy.
